# Kafka Library for ACR Simulator

---

- consumer -> reads message
- producer -> has methods for creating topics and producing message
- schema -> defines the schema of the message

#### messages must be serialized to json before sending

#### attached docker-compose to launch kafka broker and zookeeper as docker containers. `Host:Port = localhost:9092`
